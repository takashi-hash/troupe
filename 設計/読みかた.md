# Ichiza（一座）— 設計の読みかた

**版**: v0.14（2026-08-21。v0.14: 語の出どころ.md を起こし、歩1のやり直しを始めた——**用語集の299語のうち、人が口にした語は17語しかなかった**。残りは AI の発明で、名前が役割と使い道と画面を指していた。v0.13: v0.13: 実装計画.md を起こした——実装の段がどこにも書かれておらず、会話の中にしか無かった。v0.12: v0.12: 案内を実物に合わせた（層割当て表 §5 構成要素の置き場と執行者・集約境界図 §6 集約の仕立て）。v0.11: v0.11: 掟9 の範囲を明記——ドメインの語を名乗る識別子は全層、道具の構造は登録しない。宣言だけでなく執行者（対訳 lint）も全層へ広げた。v0.10: v0.10: 掟9に「構造の名前」を戻した——v0.5 の書き直しで落としていた。lint が執行者。v0.9: v0.9: いちばん上の眺めに カスタム/ と 起動/ を。v0.8: v0.8: 10_画面 は読む5枚。v0.7: v0.7: 掟10 の改名を実地で1回まわし、画面の語 lint を執行者に。v0.5: コードは英語・対訳は1箇所。v0.6: 掟9の最終形——設計文書も識別子で書く。日本語の語は用語集・画面・docstring に住む）
**目的**: 実装と設計を同じリポジトリに同居させる。設計は `設計/` の下に集め、**一目で設計だと分かる**ようにする。

---

## 1. いちばん上の眺め

```
Ichiza/
├── README.md          ← 入口。設計への案内だけ置く
├── 設計/              ← 設計はすべてここ。コードは1行も置かない
├── domain/ app/ adapters/ ui/ tests/
│                      ← 実装はリポジトリ直下（層の割当ては 7_配置/形態マップ.md §3）
├── custom/<題材>/     ← 注入するデータ（board.toml と rules/*.toml）。コードにしない
└── launchd/           ← 起動の宣言ファイル（I8「起動も自律の一部」の執行者）
```

---

## 2. 設計/ の中身

フォルダの番号は [モデリング計画.md](モデリング計画.md) の歩番号。**番号を追えば進行がそのまま見える。**

```
設計/
├── 読みかた.md                  ← この構成の説明（いま読んでいるこれ）
├── ドメインモデル.md            ← 背骨。公理・役割・Job の一生・不変条件・教訓
├── モデリング計画.md            ← 7歩の進行計画と各歩の完了条件（歩1〜7＝設計）
├── 実装計画.md                  ← 段A〜Hの順番と各段の完了条件。**順番の根拠つき**
│
├── 1_言葉/
│   ├── 語の出どころ.md          ← **語がどこから来たか**。人の発言の引用。
│   │                              出どころの無い語は用語集に入れない
│   └── 用語集.md                ← 語・一言定義・識別子。**日本語⇄識別子の唯一の橋**
│                                  （2026-08-21、作り直し中——299語のうち
│                                   出どころのあるものは17語だった）
│
├── 2_境界/
│   ├── 三分類表.md              ← CoreDomain・Supporting・Generic。「買う」判断もここ
│   └── コンテキストマップ.md    ← Foundation↔Custom↔Source の関係と ReadPort の位置
│
├── 3_部品/
│   └── 部品一覧.md              ← 語ごとに同一性/値の判定・根拠・迷いの記録
│
├── 4_集約/
│   ├── 集約境界図.md            ← Aggregate の境界と ID 参照の図。集約の仕立て（§6）
│   └── 不変条件対応表.md        ← I1〜I8 → 守る場所 → 即時/結果 → 破れたときの見え方
│
├── 5_出来事/
│   ├── 出来事カタログ.md        ← 全 Event を過去形の名前で
│   ├── 調停図.md                ← Manager の回す輪
│   └── 規則の三分解表.md        ← 不変条件・仕様・ポリシーの仕分け
│
├── 6_型/
│   ├── 状態モデル.md            ← Job の一生の型スケッチ（コードとほぼ1対1）
│   └── 禁止状態一覧.md          ← 「書けてはいけない」状態の列挙
│
├── 7_配置/
│   ├── 層割当て表.md            ← domain/app/adapters/ui。依存は内向きのみ。
│   │                              **構成要素ごとの置き場と執行者**（§5）
│   └── 形態マップ.md            ← どの領域を作り込み、何を買い、何を作らないか
│
├── 8_保存/帳簿の保存.md         ← 実装期: Ledger の SQLite（スキーマ・原子性・Origin 一意）
├── 9_働き手/働き手とLLM.md      ← 実装期: Worker の実行ループ・FramePrompt・LlmPort/MCP・Evaluation
└── 10_画面/画面.md              ← 実装期: 読む5枚と Chat の作り・導出・検索・語 lint
```

番号 8〜10 は実装期の設計（7歩の続き）。掟はそのまま効く——識別子で書く・出典なし・新語はまず用語集・空ファイルを先に置かない。

`決定記録/` というフォルダは**既定では存在しない**。決定は成果物の中に書く。独立した記録に昇格できる条件は §4。

---

## 3. 置き場の掟

1. **設計はすべて `設計/` の下。** ルート直下に設計ファイルを増やさない（README を除く）。
2. **設計ファイルは和名・全部 .md。** 図は mermaid で md の中に埋める（1成果物＝1ファイルで完結させる）。
3. **参照は前向きのみ。** 後の歩のファイルは前の歩を参照してよい。逆は不可（用語集は Aggregate を知らない）。
4. **空ファイルを先に置かない。** ファイルは歩が来たときに初めて生まれる。フォルダも歩の開始時に切る（教訓5「効かない機能は残さない」・教訓1「宣言に執行者がいなければ実装ではない」）。
5. **決定は、それが効く成果物の中に書く。** 独立した決定記録は既定で作らない。作れるのは §4 の3条件を満たすときだけ。
6. **Custom の設計は混ぜない。** 題材の設計は着手時に `カスタム/<題材>/設計/` を切る（まず `カスタム/運転/`。順はドメインモデル §10——運転→庶務→訪問診療。Foundation は中身を1行も知らない——§8 の線をフォルダでも守る）。
7. **実装は歩7のあとに、リポジトリ直下で始める**（`設計/` と `カスタム/` 以外が実装。層のフォルダは形態マップ §3）。実装中に湧いた設計の疑問は、実装の中で解かずドメインモデル.md §11（未決）へ戻す。
8. **出典を載せない。** 参考にした教材・本・他のプロジェクトの名は設計に書かない。設計は自分の言葉だけで立つ。出典はあくまで参考であり、リポジトリの外に置く。
9. **言葉の正本は用語集。設計とコードはプログラムの言葉で書く。**（前の掟9「設計は一座の言葉」を名指しで覆す）日本語の語と識別子の対を持つのは**用語集だけ**。他の設計文書とコードは、術語を**識別子のまま**書く——地の文の文法は日本語、動詞は「approve する」の形でよい。勝手訳の禁止——訳は用語集の対訳が1箇所で決める。
   **登録が要るのは「ドメインの語を名乗る識別子」だけ**（全層。`FileSource`＝ファイルの源、
   `morning_sections`＝今日の節）。**道具の構造の名は登録しない**（`SqliteLedger`・`TomlCustom`・
   `Card`・`MainWindow`——技術と役割の名で、ドメインの語ではない。3つ目の線と同じ理由）。
   執行者は `tests/glossary_lint.py`——2026-08-21 まで domain/ と app/ しか見ておらず、
   その隙に adapters/ で6つ登録なしに作った。いまは全層を見る。登録しない名は lint の `TOOL_NAMES` に
   理由を1行添えて並び、**その抜け道が効くのは adapters/ と ui/ だけ**（domain と app に道具は無いので、
   そこで `populate` と名乗れば赤）。型と関数の docstring に日本語の語を書く——grep の錨: `grep Checkpoint` は設計・実装・テストに、`grep 承認待ち` は用語集と docstring に当たる。
   **名前の置き場は3つに割れる**（2026-08-21。v0.5 の書き直しでこの一文を落とし、`業務ルール/` という
   フォルダを作って掟を破ったので、執行者つきで戻した——`tests/paths_lint.py`）:
   - **人が読む文書の置き場は和名**（掟2。`設計/`）
   - **動く仕組みの構造は英語**——フォルダ・パッケージ・設定ファイル・層の名（`custom/` `rules/`
     `board.toml` `launchd/`）。**ドメインの語を入れ物の名前に使わない**（`業務ルール/` は禁止）
   - **どれを指すかの名はデータなので現場の言葉**（`custom/運転/`・`rules/週次の検査の見張り.toml`）
     ——ボードIDや業務ルールの名と同じ扱い。中身であって構造ではない
10. **画面は別名を作らない。出す語を選ぶだけ。** 画面に出るのは**日本語の語**（用語集の語そのまま。同義語ゼロは画面にも効く）。出してよい語は用語集 §10 の一覧が決め、無い語（Lease・Core・Fingerprint…）は画面に出さない。語が画面に不向きだと分かったら、画面で言い換えず**用語集で改名する**（2026-08-21 に実際にそうした——実地で「読みにくい」と分かり、用語集 v4.0 で日本語の語を一般的な業務システムの語へ一斉に改名。識別子は1つも動かず、コードとテストは無傷だった）。**執行者は `tests/ui_words_lint.py`**——画面の表示が §10 に無ければ赤。画面に出る**中身**は現場の言葉（題材ごと——運転ならリポジトリや依存、診療なら施設や担当医）で、Constitution から来る。

> まとめ: **画面と会話は日本語の語。設計文書とコードは識別子。橋は用語集の1冊だけ。**

---

## 4. 決定記録の掟 ―― 既定では作らない

決定記録は大抵読まれなくなる。読まれない記録は嘘になる（教訓7「狼少年を作らない」・教訓5「効かない機能は残さない」）。だから:

**決定は、それが効く成果物の中に書く。** 読み返す場所に置かなければ読まれない（教訓1——記録の執行者は読む者）。

- 「認証は買う」→ `三分類表.md` に
- 「履歴を Event の列では持たない」→ `出来事カタログ.md` の冒頭に
- 「実装言語は◯◯」→ `層割当て表.md` の冒頭に

独立した決定記録に昇格できるのは、次の**3つ全部**を満たすときだけ:

1. **覆すと高くつく** —— やり直しがディレクトリ移動で済まない（データ移行・書き直し級）
2. **1つの成果物に収まらない** —— 複数の歩・複数のファイルにまたがって効く
3. **読む者と読む時が言える** —— 「誰が・いつ読み返すか」を1行で書ける。書けなければ作らない

3つ揃って初めて `決定記録/` を切る（それまでフォルダも無い）。作ったものは積むだけ・上書きなし。覆すときは新しい決定で前の決定を名指しで覆す。

---

## 5. いつ何が生まれたか（歩との対応）

| 歩 | 生まれたファイル |
|---|---|
| 1 | `1_言葉/用語集.md` |
| 2 | `2_境界/三分類表.md`・`コンテキストマップ.md` |
| 3 | `3_部品/部品一覧.md` |
| 4 | `4_集約/集約境界図.md`・`不変条件対応表.md` |
| 5 | `5_出来事/` の3枚（履歴の持ち方の結論は出来事カタログの冒頭に） |
| 6 | `6_型/状態モデル.md`・`禁止状態一覧.md` |
| 7 | `7_配置/層割当て表.md`・`形態マップ.md`（実装言語の決定は層割当て表の冒頭に） |
