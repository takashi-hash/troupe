# 源の対応表 — どの読み口が、実際に何を読むか。
# 土台は「検査の結果」が何かを知らない。ここがカスタムのデータ（設計/形態マップ）。

[[sources]]
ref = "読み口/検査の結果"
kind = "command"
command = "./check.sh 2>&1 | tail -20"

[[sources]]
ref = "読み口/依存の一覧"
kind = "file"
path = "pyproject.toml"

[[sources]]
ref = "読み口/設計文書"
kind = "file"
path = "設計"
