# 一座で作業するとき

**まず `設計/どう作るか.md` を読む。** 掟・層・フォルダ・赤くする仕掛けの正本。

- 設計は5枚・1100行まで。足すなら何かを消す。数の正本は1つ（掟3）
- **表に無いファイルを作らない**——作るなら先に設計のフォルダ表へ行を足す
- まとめかたはパターンの名（value_objects・aggregates・events・services…）、
  **中の1ファイルは業務の1概念**。役割の名は DDD の定訳のまま使う
- 変更前に机上ロールプレイで「設計が要る／実装で決める」を分け、
  設計が要るものは**先に設計を直してから**コードを書く
- 動かして分かったことは設計に戻す。出典（教材名・他プロジェクト名）は設計に書かない

検査（コミット前に全部）:

```sh
uv run pytest -q && uv run pyright && uv run lint-imports
uv run python tests/break_check.py   # 義務を壊して全部赤くなるか（時間がかかる）
```

突合テストが設計の表とコードを照合している——**設計の .md を書き換えると赤くなる**
（遷移表・出来事・値オブジェクト・§1 の操作表・§4 の interface 表）。ずれたらどちらが正本か考えてから直す。正本は設計。
